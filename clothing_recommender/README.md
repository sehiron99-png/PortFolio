# clothing_recommender

`init.py` : 해당 폴더를 Python 패키지로 인식하게 해주는 설정 파일

`cli.py` : 명령줄 인터페이스를 처리하는 파일, 터미널/콘솔을 통해 사용자의 입력을 받고 결과를 출력하는 구동 로직됨

`recommender.py` : 의류 추천 알고리즘 및 데이터를 기반으로 실제 추천 로직을 수행하는 핵심 코드가 담긴 파일
